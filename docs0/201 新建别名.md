我看到您有一个完整的 Beancount v3 云端开发环境配置脚本。让我为您创建一个专门的 Git 推送脚本，集成到您的环境中。

## 创建 Git 工具脚本

```bash
# 创建 Git 工具目录
mkdir -p /workspace/.scripts/git-tools

# 创建 Git 推送脚本
cat > /workspace/.scripts/git-tools/git_push.sh << 'EOF'
#!/bin/bash

# Git 自动推送脚本 - 专为 Beancount v3 环境设计
SCRIPT_DIR="$(dirname "$(realpath "$0")")"
PUSH_COUNT_FILE="$SCRIPT_DIR/.push_count"
BEANCOUNT_REPO="/workspace/data/beancount/3978d009748ef54ad6ef7bf851bd55491b1fe6bb"

# 设置颜色输出
GREEN='\033[0;32m'
YELLOW='\033[0;33m'
RED='\033[0;31m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

# 切换到仓库目录
cd "$BEANCOUNT_REPO" || { 
    echo -e "${RED}❌ 错误: 无法进入 Beancount 仓库目录${NC}"
    echo -e "请检查路径: $BEANCOUNT_REPO"
    exit 1
}

# 检查是否有更改
if [ -z "$(git status --porcelain)" ]; then
    echo -e "${YELLOW}📝 没有需要提交的更改${NC}"
    exit 0
fi

# 读取或初始化推送次数
if [ -f "$PUSH_COUNT_FILE" ]; then
    PUSH_COUNT=$(cat "$PUSH_COUNT_FILE")
else
    PUSH_COUNT=0
fi

PUSH_COUNT=$((PUSH_COUNT + 1))
echo "$PUSH_COUNT" > "$PUSH_COUNT_FILE"

# 生成提交信息
TIMESTAMP=$(date +"%Y-%m-%d %H:%M:%S")
BRANCH=$(git branch --show-current)
COMMIT_PREFIX="beancount"

# 如果有参数，使用参数作为提交信息
if [ $# -gt 0 ]; then
    COMMIT_MSG="$*"
else
    # 自动检测更改类型
    CHANGES=$(git status --porcelain)
    if echo "$CHANGES" | grep -q "month/.*\.bean"; then
        COMMIT_MSG="$COMMIT_PREFIX: 更新月度账本 - $TIMESTAMP"
    elif echo "$CHANGES" | grep -q "\.bean"; then
        COMMIT_MSG="$COMMIT_PREFIX: 更新账本文件 - $TIMESTAMP"
    else
        COMMIT_MSG="$COMMIT_PREFIX: 第$PUSH_COUNT次推送 - $TIMESTAMP"
    fi
fi

echo -e "${BLUE}🚀 Beancount Git 推送操作${NC}"
echo -e "${BLUE}========================================${NC}"
echo -e "📋 分支: ${GREEN}$BRANCH${NC}"
echo -e "💬 提交信息: ${YELLOW}$COMMIT_MSG${NC}"
echo -e "${BLUE}========================================${NC}"

# 显示更改摘要
echo -e "📊 更改摘要:"
git status --short

# 执行 Git 操作
echo -e "\n1. ${BLUE}添加文件...${NC}"
git add . || { echo -e "${RED}❌ git add 失败${NC}"; exit 1; }

echo -e "2. ${BLUE}提交更改...${NC}"
git commit -m "$COMMIT_MSG" || { echo -e "${RED}❌ git commit 失败${NC}"; exit 1; }

echo -e "3. ${BLUE}推送到远程...${NC}"
git push || { echo -e "${RED}❌ git push 失败${NC}"; exit 1; }

echo -e "${BLUE}========================================${NC}"
echo -e "${GREEN}✅ 推送完成!${NC}"
echo -e "📈 第 ${YELLOW}$PUSH_COUNT${NC} 次推送"
echo -e "⏰ 时间: ${YELLOW}$TIMESTAMP${NC}"
echo -e "🌿 分支: ${GREEN}$BRANCH${NC}"
echo -e "${BLUE}========================================${NC}"
EOF

# 创建 Git 状态检查脚本
cat > /workspace/.scripts/git-tools/git_status.sh << 'EOF'
#!/bin/bash

# Beancount Git 状态检查脚本
BEANCOUNT_REPO="/workspace/data/beancount/3978d009748ef54ad6ef7bf851bd55491b1fe6bb"

# 设置颜色输出
GREEN='\033[0;32m'
YELLOW='\033[0;33m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

# 切换到仓库目录
cd "$BEANCOUNT_REPO" || { 
    echo -e "${RED}❌ 错误: 无法进入 Beancount 仓库目录${NC}"
    exit 1
}

echo -e "${BLUE}📊 Beancount 仓库状态${NC}"
echo -e "${BLUE}========================================${NC}"

# 显示分支信息
echo -e "🌿 当前分支: ${GREEN}$(git branch --show-current)${NC}"
echo -e "📡 远程跟踪: ${YELLOW}$(git remote -v | grep origin | head -1 | awk '{print $2}')${NC}"

echo -e "${BLUE}----------------------------------------${NC}"

# 显示状态
git status

echo -e "${BLUE}========================================${NC}"
EOF

# 创建 Git 日志查看脚本
cat > /workspace/.scripts/git-tools/git_log.sh << 'EOF'
#!/bin/bash

# Beancount Git 日志查看脚本
BEANCOUNT_REPO="/workspace/data/beancount/3978d009748ef54ad6ef7bf851bd55491b1fe6bb"

# 设置颜色输出
BLUE='\033[0;34m'
NC='\033[0m' # No Color

# 切换到仓库目录
cd "$BEANCOUNT_REPO" || exit 1

echo -e "${BLUE}📜 Beancount 提交历史${NC}"
echo -e "${BLUE}========================================${NC}"

# 显示简洁的提交历史
git log --oneline --graph -10 --color=always

echo -e "${BLUE}========================================${NC}"
EOF

# 给脚本执行权限
chmod +x /workspace/.scripts/git-tools/*.sh

# 初始化推送计数
echo "0" > /workspace/.scripts/git-tools/.push_count
```

## 创建别名安装脚本

```bash
# 创建 Beancount 专用别名脚本
cat > /workspace/.scripts/beancount-aliases.sh << 'EOF'
#!/bin/bash

# Beancount 专用别名配置
echo "设置 Beancount Git 快捷命令..."

# Git 相关别名
alias gs="/workspace/.scripts/git-tools/git_status.sh"
alias gp="/workspace/.scripts/git-tools/git_push.sh"
alias gl="/workspace/.scripts/git-tools/git_log.sh"

# 标准 Git 命令别名
alias ga="git add"
alias gc="git commit"
alias gd="git diff"
alias gpl="git pull"

# 目录导航别名
alias cdbean="cd /workspace/data/beancount/3978d009748ef54ad6ef7bf851bd55491b1fe6bb"
alias cdscripts="cd /workspace/.scripts"
alias cdwork="cd /workspace"

# 显示帮助信息
alias githelp="echo -e '\033[0;34m📚 Beancount Git 快捷命令:\033[0m\n  gs - 查看状态\n  gp - 自动推送\n  gl - 查看日志\n  ga - 添加文件\n  gc - 提交更改\n  gd - 差异比较\n  gpl - 拉取更新'"

echo "✅ Beancount Git 别名设置完成"
echo "输入 'githelp' 查看可用命令"
EOF

# 给脚本执行权限
chmod +x /workspace/.scripts/beancount-aliases.sh
```

## 修改您的启动脚本

在您的 `start_on_cnb.sh` 脚本末尾添加以下内容：

```bash
# 在脚本末尾添加以下内容

# 设置 Git 工具别名
setup_git_aliases() {
    echo "设置 Git 快捷命令..."
    source /workspace/.scripts/beancount-aliases.sh
}

# 在主函数末尾调用
echo "配置 Git 工具..."
setup_git_aliases

echo "Git 工具配置完成!"
echo "使用 'gp' 命令快速推送 Beancount 更改"
echo "使用 'githelp' 查看所有可用命令"
```

## 使用方法

现在您可以使用以下快捷命令：

### 1. 查看状态
```bash
gs
```

### 2. 自动推送（智能生成提交信息）
```bash
gp
```

### 3. 自定义提交信息推送
```bash
gp "添加9月餐饮支出"
```

### 4. 查看提交历史
```bash
gl
```

### 5. 查看帮助
```bash
githelp
```

## 脚本特点

1. **智能提交信息**: 自动检测更改类型并生成合适的提交信息
2. **推送计数**: 记录推送次数并在提交信息中显示
3. **颜色输出**: 使用彩色输出提高可读性
4. **错误处理**: 完善的错误检查和提示
5. **集成环境**: 专为您的 Beancount v3 环境设计

这些脚本已经集成到您的开发环境中，每次启动都会自动加载别名，让您的 Git 工作流程更加高效！